<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Moonrise.Controls.CircleIconButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Moonrise.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="FilledIconButton" TargetType="Button">
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style x:Key="TransparentIconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
        </Style>

        <Style x:Key="ActiveIcon" TargetType="FontIcon">
            <Setter Property="Foreground" Value="{ThemeResource AccentFillColorDefaultBrush}"/>
        </Style>
        <Style x:Key="InactiveIcon" TargetType="FontIcon">
            <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
        </Style>
    </UserControl.Resources>
    <Button x:Name="CircleIconButtonObject" CornerRadius="64" Width="{x:Bind Size, Mode=OneWay}" Height="{x:Bind Size, Mode=OneWay}" BorderThickness="1" BorderBrush="{ThemeResource ControlStrokeColorSecondary}">
        <FontIcon x:Name="GlyphIcon" Glyph="{x:Bind Glyph, Mode=OneWay}" FontSize="{x:Bind IconSize, Mode=OneWay}"/>
    </Button>
</UserControl>

<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Moonrise.Controls.TrackListItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Moonrise.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="FavoriteStates">
                <VisualState x:Name="NotFavorite">
                    <VisualState.Setters>
                        <Setter Target="FavoriteIconDisplay.Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Favorite">
                    <VisualState.Setters>
                        <Setter Target="FavoriteIconDisplay.Foreground" Value="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Button Background="Transparent"
                BorderThickness="0"
                Padding="6"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                Click="Button_Click">
            <Button.ContextFlyout>
                <MenuFlyout Opening="MenuFlyout_Opening">
                    <MenuFlyoutItem Text="Play next" Icon="Import" Click="PlayNext_Click"/>
                    <MenuFlyoutItem Text="Play last" Click="PlayLast_Click">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xEA52;"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    <MenuFlyoutItem x:Name="FavoriteMenuItem" Text="Favorite" Click="Favorite_Click">
                        <MenuFlyoutItem.Icon>
                            <FontIcon x:Name="FavoriteIcon" Glyph="&#xEB51;"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                </MenuFlyout>
            </Button.ContextFlyout>
            <Grid Padding="6,6,12,6" HorizontalAlignment="Stretch" ColumnSpacing="24" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="60"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{x:Bind FormatTrackNumber(Song.TrackNumber), Mode=OneWay}" Style="{StaticResource BodyTextBlockStyle}" Visibility="{x:Bind TrackViewVisibility, Mode=OneWay}" VerticalAlignment="Center" MinWidth="24" Margin="6,0,0,0"/>

                <Grid Grid.Column="1" CornerRadius="4" Height="40" MaxWidth="40" Visibility="{x:Bind ArtViewVisibility, Mode=OneWay}">
                    <Image Source="{x:Bind DisplayedCoverArt, Mode=OneWay, TargetNullValue='ms-appx:///Assets/Placeholder.png'}" Stretch="UniformToFill"/>
                </Grid>

                <StackPanel Grid.Column="2" VerticalAlignment="Center" Spacing="2">
                    <TextBlock Text="{x:Bind Song.Title, Mode=OneWay}" Style="{StaticResource BodyStrongTextBlockStyle}" TextTrimming="CharacterEllipsis"/>
                    <TextBlock Text="{x:Bind Song.Artist, Mode=OneWay}" Style="{StaticResource CaptionTextBlockStyle}" TextTrimming="CharacterEllipsis"/>
                </StackPanel>

                <TextBlock Grid.Column="3" Text="{x:Bind Song.Album, Mode=OneWay}" Style="{StaticResource BodyTextBlockStyle}" VerticalAlignment="Center" HorizontalAlignment="Left" TextTrimming="CharacterEllipsis" Visibility="{x:Bind DisplayAlbumVisibility, Mode=OneWay}"/>

                <FontIcon x:Name="FavoriteIconDisplay" Grid.Column="4" Glyph="&#xEB51;" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>

                <TextBlock Grid.Column="5" Text="{x:Bind FormatDuration(Song.Duration), Mode=OneWay}" Style="{StaticResource BodyTextBlockStyle}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>
        </Button>
    </Grid>
</UserControl>

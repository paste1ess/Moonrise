<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Moonrise.Pages.LyricsPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Moonrise.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Margin="0, 16, 16, 0" Spacing="8" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Canvas.ZIndex="2">
            <DropDownButton x:Name="DropDown">
                <DropDownButton.Flyout>
                    <MenuFlyout Placement="Bottom">
                        <MenuFlyoutItem Text="Static" Click="Static_Click"/>
                        <MenuFlyoutItem Text="Synced (line by line)" Click="Synced_Click"/>
                    </MenuFlyout>
                </DropDownButton.Flyout>
            </DropDownButton>
            <Button Width="32" Height="32" Padding="0">
                <Button.Flyout>
                    <MenuFlyout Placement="BottomEdgeAlignedRight">
                        <MenuFlyoutItem Text="Edit Lyrics" Click="EditLyrics_Click">
                            <MenuFlyoutItem.Icon>
                                <SymbolIcon Symbol="Edit" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="Mark as Instrumental" Click="MarkInstrumental_Click">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xEC54;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="Clear Lyrics" Click="ClearLyrics_Click">
                            <MenuFlyoutItem.Icon>
                                <SymbolIcon Symbol="Clear" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </Button.Flyout>

                <FontIcon Glyph="&#xE712;" />
            </Button>
        </StackPanel>

        <Frame x:Name="ContentFrame" IsNavigationStackEnabled="False" Background="Transparent" BorderThickness="0"/>
    </Grid>
</Page>
